import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#f78104',
      contrastText: '#fff',
      borderColor: '#fff'
    }
  }
});

export default theme;